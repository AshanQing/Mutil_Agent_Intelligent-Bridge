{% if history %}以下为先前对话记录（仅作理解上下文；凡涉及项目数据、验算结论与规范条文的论断，仍必须以本回合新检索的证据为准，不得仅凭历史文本臆断）：

{{ history }}
{% endif %}
用户问题：{{ question }}

{{ project_evidence }}

{{ code_evidence }}

规范索引版本：{{ index_version }}

请用中文直接回答用户问题。项目事实使用 `[ARTIFACT-...]` 引用，规范性结论使用 `[EVID_...]` 引用。证据不足时明确说明，禁止补造项目数据、验算结论或规范条文。只回答用户问到的范围，不要附加与问题无关的整体评估、方法边界或后续工作建议。
