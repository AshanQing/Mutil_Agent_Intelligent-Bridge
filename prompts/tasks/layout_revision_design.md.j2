# 设桥布跨方案修正任务

## 1. 角色定位
{{ROLE_BLOCK}}

你当前执行的是“设桥布跨方案修正任务”，不是重新从零生成方案。
必须以上一次设桥布跨结果为基础，根据修正建议进行最小必要修改。

## 2. 设计规范与约束
{{STANDARDS_BLOCK}}

## 3. 修正原则
1. 优先执行修正建议中的具体要求。
2. 不得仅修改“避让策略”“墩位校核说明”“主要风险点”等说明性文本作为修正结果。
3. 当前 collision_detection_tool 主要读取桥跨范围、跨径组合、总跨数、桥梁总长和墩位桩号等机器字段。
4. 因此，修正必须实际改变上述机器可读字段。
5. 若为连续多墩冲突，必须先确认报告包含可验证的连续障碍物边界；缺少该边界时转人工复核，不得根据碰撞墩位的纵向包络推导净跨径、删除中间墩或新增大跨结构。
6. 若为桥台或桥梁起终点冲突，必须调整桥梁起终点桩号。
7. 若为孤立墩位冲突，应通过调整前后相邻跨径使该墩沿纵向移动至安全侧。
8. 修正后必须保持桥梁编号、墩台编号、跨径组合、总跨数、桥梁总长、墩位桩号等字段前后一致。
9. 输出结果必须能够被 collision_detection_tool 直接读取。

## 4. 输出格式
{{OUTPUT_SCHEMA_BLOCK}}

## 5. 当前线路信息
```json
{{DESIGN_INPUT_JSON}}
```

## 6. 初步设计结果或上一轮修正结果
```json
{{PREVIOUS_LAYOUT_JSON}}
```

## 7. 紧凑碰撞指标与修正决策记录
```json
{{REVISION_ADVICE_JSON | default("{}")}}
```

## 8. 修正建议
{{REVISION_INSTRUCTION_TEXT}}

## 9. 最终输出要求
只输出修正后的完整 JSON 对象。
不得输出 Markdown。
不得包裹代码块。
不得输出解释性文字。
